let all_fruits = ['red', 'green', 'red', 'green','red', 'green', 'red', 'green'];
let ripen_fruits = []

all_fruits.forEach(function(element) {
    if (element ==='red'){
        ripen_fruits.push(element)
    }
  });
console.log(ripen_fruits);


