def ai(args):
  ripen_fruits = []

  for color in args:
    if color == 'red':
        ripen_fruits.append(color)

  print(ripen_fruits)

fruits = ['red', 'green', 'red', 'green','red', 'green', 'red', 'green', 'yellow']

ai(fruits)
